# Daily Log: 2026-02-13

## MLM Plateau Around Loss ~5: Additional Convergence Pass
- User report: MLM pretraining was plateauing near loss ~5.
- Added ambiguity-aware MLM masking controls:
  - `mlm.mask_ambiguous_tokens` (default `false`): allows excluding `N`-containing tokens from supervised mask targets.
  - `mlm.min_masked_tokens` (default `2` in repo config): enforces minimum supervised targets per sample.
- Updated dataset masking implementation:
  - explicit `maskable_token_ids` filtering
  - replacement-token pool aligned with maskable token set
  - deterministic forced masking for under-masked samples.
- Updated default pretrain recipe for lower final loss:
  - `lr: 2e-4`
  - `epochs: 320`, `min_epochs: 80`
  - `early_stopping_patience: 50`, `early_stopping_min_delta: 0.0002`
  - `samples_per_epoch: 90000`
  - `grad_accum_steps: 3`
  - `dropout: 0.05`
- Updated checklist and tests to match new defaults/options.
- Validation complete: full suite green (`35 passed`).
