# Daily Log: 2026-02-10

## Colab Stability + Throughput Improvements for MLM Pretraining
- Investigated pretraining interruptions that appeared around tokenizer startup in Colab.
- Identified high-risk stage: BPE tokenizer training over full multi-billion-base corpus with no intermediate progress output.
- Implemented tokenizer hardening:
  - Added verbose BPE merge progress logging (`tokenization/bpe.py`).
  - Added tokenizer corpus sampling controls in pretrain config parsing (`tokenizer_max_bases`, `tokenizer_max_sequences`, `tokenizer_window_size`).
  - Added deterministic tokenizer corpus sampler in pretraining script.
- Preserved full MLM training path on complete genome corpus; tokenizer sampling only affects vocabulary induction stage.
- Updated default pretrain config with tokenizer sampling limits suitable for Colab reliability.
- Added/updated tests for config parsing defaults and tokenizer sampling limits.
- Validation complete: test suite green (`26 passed`).
