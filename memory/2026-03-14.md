# Daily Log: 2026-03-14

## Read Today + Yesterday
- Read `MEMORY.md` and `memory/2026-03-13.md` before changing the pretraining stack.
- Continued investigating the persistent MLM loss plateau around `~6` after the earlier window-length and tokenizer-metadata fixes.

## Work Completed
- Found a concrete tokenizer bug in `sample_tokenizer_corpus()`:
  - it only sampled one window per FASTA record
  - large genomes therefore contributed a single slice to BPE training
  - tokenizer vocab quality was lower than expected for whole-genome pretraining
- Fixed tokenizer corpus sampling to:
  - sample windows with replacement
  - weight sequence selection by contig/genome length
  - keep sampling until `tokenizer_max_bases` / `tokenizer_max_sequences` budgets are actually reached
- Extended tokenizer metadata matching so existing tokenizer files are retrained when corpus-sampling settings change:
  - `tokenizer_window_size`
  - `tokenizer_max_bases`
  - `tokenizer_max_sequences`
- Reduced default tokenizer corpus budgets in `pretrain.yaml` because the current pure-Python BPE trainer is intentionally simple and should not be asked to process tens of millions of bases.
- Added transformer-side optimization fixes:
  - embedding `LayerNorm`
  - BERT-style small-std initialization for embeddings, linear layers, attention projections, and MLM head parameters
- Added/updated regression tests for:
  - multi-window tokenizer corpus sampling from a single long sequence
  - tokenizer retraining on corpus-sampling metadata mismatch
  - transformer embedding normalization / pad-row initialization
  - MLM model tied/untied head behavior
- Implemented structured pretraining diagnostics:
  - `run_manifest.json`
  - `preflight_profile.json`
  - `metrics.jsonl`
  - `epoch_metrics.csv`
- Added preflight MLM profiling helpers and CLI:
  - `gene_whisperer/training/pretrain_profiler.py`
  - `gene_whisperer/training/profile_mlm_corpus.py`
- Pretraining now records epoch-level signal quality metrics including:
  - tokenized-length distributions
  - maskable-token rates
  - masked-token rates
  - skipped-batch rates
  - supervised-token totals
  - token-weighted loss
- `MLMDataset` now exposes `maskable_count`, `tokenized_count`, and `masked_count` per sample for diagnostics.

## Validation
- `python -m pytest -q`
- Result: `51 passed in 1.62s`

## Automation Notes
- Reviewed recent pretraining-related commits and local project memory to extract skill-progression recommendations grounded in recurring PR themes.
- Main recurring themes across the latest PRs:
  - repeated MLM loss plateau fixes across config, dataset sampling, tokenizer training, and transformer initialization
  - repeated hardening of config-to-artifact contracts so saved tokenizers do not silently invalidate new experiments
  - repeated addition of narrow regression tests after each pretraining/runtime bug was isolated
- Recommended next skills to deepen:
  - experiment instrumentation for MLM/pretraining loops
  - artifact/config contract design
  - hypothesis-driven regression test design for data + training pipelines
