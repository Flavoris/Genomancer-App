# Daily Log: 2026-02-12

## MLM Loss Reduction Pass
- Goal: push MLM pretraining below prior plateau (~6.36 loss) and make stopping behavior less premature.
- Added optimization upgrades:
  - warmup+cosine LR schedule
  - AMP control and gradient scaler helpers
  - gradient clipping support from config.
- Upgraded optimizer behavior:
  - default AdamW now uses no-decay group for bias/norm/vector params.
- Upgraded MLM architecture:
  - replaced simple norm+linear head with BERT-style MLM transform head (`Linear -> GELU -> LayerNorm -> Dropout`) plus decoder bias.
  - kept embedding/decoder weight tying.
- Upgraded data sampling:
  - per-worker RNG seeding in MLM dataset to avoid repeated random streams across dataloader workers.
  - optional genome length-weighted sampling (`mlm.sample_by_length: true`).
  - masking now excludes reserved tokens for random replacement and forces at least one supervised mask per sample.
- Strengthened default pretraining recipe in `pretrain.yaml`:
  - larger encoder (`embedding_dim: 320`, `num_layers: 8`, `ff_dim: 1280`)
  - lower LR (`3e-4`), larger effective batch (`grad_accum_steps: 2`)
  - longer schedule (`epochs: 240`, `min_epochs: 60`, `patience: 30`, smaller `min_delta`)
  - increased sample budget per epoch.
- Updated tests and checklist expectations; full suite green (`32 passed`).
