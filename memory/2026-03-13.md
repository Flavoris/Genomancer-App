# Daily Log: 2026-03-13

## Read Today + Yesterday
- Reviewed current MLM convergence work, dataset masking controls, and tokenizer/runtime behavior.
- Confirmed pretraining still reused old tokenizer artifacts if a tokenizer file already existed, which blocked newer tokenizer settings from taking effect in Colab.

## Work Completed
- Added tokenizer metadata persistence to the BPE tokenizer.
- Added tokenizer training controls:
  - `tokenizer_min_freq`
  - `tokenizer_max_token_length`
  - `tokenizer_retrain_if_mismatch`
- Updated pretraining runtime to retrain tokenizer automatically when saved tokenizer metadata does not match the requested config.
- Increased default tokenizer sampling budget in pretrain config to improve cross-organism vocabulary quality.
- Added regression tests for:
  - tokenizer metadata persistence
  - max token length enforcement
  - tokenizer retraining on metadata mismatch
  - tokenizer reuse on metadata match
- Identified a second pretraining issue: MLM windows were still using promoter-scale base lengths and often underfilled the BPE token budget.
- Increased default MLM pretraining window length from `234` bases to `1024` bases.
- Added `min_tokenized_tokens` so the dataset resamples short/underfilled tokenized windows.
- Updated epoch loss aggregation to be weighted by supervised token count rather than number of batches.

## Validation
- `python -m pytest -q`
- Result: `42 passed`
