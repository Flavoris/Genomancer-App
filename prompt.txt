Pre-flight Checklist

Before running, verify everything is in place:
```bash
# Check vocabularies exist
ls -la ../artifacts/vocabs/mlm_k*.json

# Check config has multi-kmer settings
python -c "
import yaml
with open('config.yaml') as f:
    cfg = yaml.safe_load(f)
print('multi_kmer_pretrain_enabled:', cfg.get('multi_kmer_pretrain_enabled'))
print('pretrain_kmers:', cfg.get('pretrain_kmers'))
print('mlm_early_stopping_enabled:', cfg.get('mlm_early_stopping_enabled'))
print('mlm_early_stopping_patience:', cfg.get('mlm_early_stopping_patience'))
"

# Check early_stopping.py exists
ls -la early_stopping.py
```