Force every eval script to print “what am I evaluating?”

add these prints at startup in:

scripts/reality_check_best_threshold.py

scripts/calibrate_stage1_temperature.py

any validation block in train_stage1.py

Print:

checkpoint path actually loaded

number of val samples

label balance (% positives)

prob min/mean/max

logit min/mean/max (if available)

If two runs disagree, those 5 lines will immediately explain why.