Final Smoke Test Before Full Retraining

Before running full retraining on Google Colab, I want to run a quick smoke test to verify everything works end-to-end.

**Run this command:**
```bash
python train_stage1.py --config config.yaml
```

**Check the logs for these key indicators:**

1. **Correct max_bp_len:**
   - Should see: "Max BP length: 81" (not 234)

2. **Correct learning rate:**
   - Should see: "Learning rate: 0.0001" or similar for stage 1

3. **Engineered features dimension:**
   - Should see: "engineered_dim=288" or similar

4. **Reverse complement enabled:**
   - Should see augmentation logs showing reverse complement is active

5. **Post-CNN transformer layers:**
   - Model summary should show 3 layers (not 6)

**If training starts successfully and first few batches run without errors, the fixes are working.**

**Optional: Run a quick ablation (few epochs) to verify accuracy is improving:**
```yaml
# Temporarily add to config.yaml for quick test:
ablation:
  enabled: true
  stage1_epochs: 5
  stage1_max_steps: 100
```

Then check if validation accuracy is already showing improvement over your previous 84-85% baseline.