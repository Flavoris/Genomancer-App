Add a “single command” smoke test to catch broken checkpoint loading

Create scripts/smoke_ablation_pipeline.py that:

loads config

turns on ablation enabled

sets tiny budgets: pretrain_max_steps=5, stage1_max_steps=5, stage2_max_steps=5, sample limits=64

runs baseline end-to-end: pretrain → stage1(load ckpt) → stage2(load ckpt)

asserts:

encoder checkpoint exists on disk

stage1_auc/mcc are finite numbers

stage2_auc/mcc are finite numbers

prints “SMOKE OK” on success

This should run in under a couple minutes.

Checkpoint:

python scripts/smoke_ablation_pipeline.py