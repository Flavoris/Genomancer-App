verify pad/mask ids end-to-end

“Trace pad_token_id and mask_id through: dataset.py -> vocab -> collators -> model forward. Add assertions:

- pad_token_id is consistent across train and val

- mask token exists in vocab and vocab.mask_id is valid

- padding mask in model uses pad_token_id correctly.
  Add unit tests for KmerVocabulary.tokenize() and _sanitize_sequence().”

Acceptance:

- Unit tests pass.

- Padding mask aligns with tokenization output.