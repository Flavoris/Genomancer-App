Add a “GPU-util sanity line” every N steps

Add lightweight periodic telemetry every 50 optimizer steps:

step time

data loading time vs compute time (use time.perf_counter() around fetch and forward/backward)

if CUDA: print torch.cuda.max_memory_allocated() and current allocated

Keep it minimal and not spammy.
Show the diff.