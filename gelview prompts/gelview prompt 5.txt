Goal: Ladder-style ticks and bp labels on the right side. Toggleable.

Actions:

1. Add a showTicks: Bool = true property to GelRenderView.

2. If true, draw tiny 6pt ticks at every 100 bp from 100 → maxBP using the same y-mapping.

3. Draw monospaced bp labels at x = laneRect.maxX + 16, font size 9, opacity 0.35.

4. Make sure labels don’t clip; extend the Canvas width if necessary or shrink lane width slightly.

Deliverable: minimalist, readable ticks and labels matching the mapping.